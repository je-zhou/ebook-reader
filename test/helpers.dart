import 'package:test_drive/domain/book/book.dart';

const imageUrl =
    'assets/hello_habits.jpg'; // prefer if i can use a dummy path :/
const title = 'title';
const author = 'author';
const exampleBook = Book(imageUrl: imageUrl, title: title, author: author);
